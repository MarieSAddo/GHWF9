def compute_hw_average(grades):
    """
    Compute the average grade for list of assignments
    Param: grades
    """
    if len(grades) == 0:
        return 0
    return grades[0]
"""
doesn't matter
"""
